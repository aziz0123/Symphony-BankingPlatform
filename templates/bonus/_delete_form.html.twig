<form method="post" action="{{ path('app_bonus_delete', {'id': bonu.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bonu.id) }}">
    <button class="btn">Delete</button>
</form>
