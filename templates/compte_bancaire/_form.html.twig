{{ form_start(form) }}
    {# {{ form_widget(form) }} #}

<div class="row">
        <div  style="display: flex;align-items: center;justify-content: center;margin-top: 150px; padding: 10px;">

            <div class="card">
                <div class="card-body">
                    <h5 class="card-title">Add CompteBancaire</h5>

                    <!-- General Form Elements -->
                    {{ form_start(form,{attr:{novalidate:'novalidate'}}) }}
                        <label for="inputText" class="col-sm-4 col-form-label">Nom</label>
                        <div class="col-sm-10">
                            {{ form_widget(form.nom_prop,{'attr':{'class':'form-control'}}) }}

                        </div>
                    {{ form_errors(form.nom_prop,{'attr':{'class':'text-danger'}}) }}



                    <label for="inputText" class="col-sm-4 col-form-label">rib</label>
                        <div class="col-sm-10">
                            {{ form_widget(form.rib,{'attr':{'class':'form-control'}}) }}

                    </div>
                    {{ form_errors(form.rib,{'attr':{'class':'text-danger'}}) }}

                    <label for="inputText" class="col-sm-4 col-form-label">solde</label>
                        <div class="col-sm-10">
                            {{ form_widget(form.solde,{'attr':{'class':'form-control'}}) }}
                        </div>
                    {{ form_errors(form.solde,{'attr':{'class':'text-danger'}}) }}

                    <br/>
                    <div class="col-sm-10" style="display: flex;align-items: center;justify-content: center">

                       <button class="btn">{{ button_label|default('Save') }}</button>
                       <a href="{{ path('app_compte_bancaire_index') }}">liste</a>
                    </div>

                    {{ form_end(form) }}

                </div>
            </div>

        </div>

    </div>



    {# <button class="btn">{{ button_label|default('Save') }}</button> #}
{{ form_end(form) }}
