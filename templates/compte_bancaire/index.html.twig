 {% extends 'base-back.html.twig' %}  

{% block title %}CompteBancaire index{% endblock %}

{% block body %}

    {# <h1><br><br>CompteBancaire index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom_prop</th>
                <th>Rib</th>
                <th>Solde</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for compte_bancaire in compte_bancaires %}
            <tr>
                <td>{{ compte_bancaire.id }}</td>
                <td>{{ compte_bancaire.nomProp }}</td>
                <td>{{ compte_bancaire.rib }}</td>
                <td>{{ compte_bancaire.solde }}</td>
                <td>
                    <a href="{{ path('app_compte_bancaire_show', {'id': compte_bancaire.id}) }}">show</a>
                    <a href="{{ path('app_compte_bancaire_edit', {'id': compte_bancaire.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_compte_bancaire_new') }}">Create</a> #}






    <div class="row" style="display: flex;align-items: center;justify-content: center;margin-top: 150px;">

    <div class="col-lg-6">

        <div class="card">
            <div class="card-body">
                <a href="{{ path('app_compte_bancaire_new') }}"><i class="ri-arrow-left-circle-fill" style="font-size: 50px;"></i></a>
                <h5 class="card-title">liste des CompteBancaire</h5>

                <!-- Table with stripped rows -->
                <table class="table table-striped">
                    <thead>
                    <tr>     
                        <th>Id</th>
                        <th>Nom_prop</th>
                        <th>Rib</th>
                        <th>Solde</th>
                        <th>Modification</th>
                        <th>Suppression</th>
                    </tr>
                    </thead>
                    <tbody>
                    {% for compte_bancaire in compte_bancaires %}
                        <tr>
                            <td>{{ compte_bancaire.id }}</td>
                            <td>{{ compte_bancaire.nomProp }}</td>
                            <td>{{ compte_bancaire.rib }}</td>
                             <td>{{ compte_bancaire.solde }}</td>
                            <td>

                            
                                <a class="btn btn-success" href="{{ path('app_compte_bancaire_edit', {'id': compte_bancaire.id}) }}">Modifier</a>
                            </td>
                            <td>
                                {{ include('compte_bancaire/_delete_form.html.twig') }}
                            </td>
                        </tr>

                    {% endfor %}

                    </tbody>
                </table>
                <!-- End small tables -->

            </div>
        </div>

    </div>



</div>



{% endblock %}
